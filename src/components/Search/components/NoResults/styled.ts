import styled from 'styled-components';

export const SearchNoResultsStyled = styled.div`
  display: block;
  padding: 10px 0;
  color: #555;
  font-style: italic;
  text-align: center;
`;

export const SearchNoResultsResetSearchStyled = styled.button`
  margin: 0 0 0 5px;
  padding: 0;
  border: 0 none;
  background: transparent;
  color: #1b1bc0;
  font-size: inherit;
  font-style: italic;
  text-decoration: underline;
  text-underline-offset: 4px;
  cursor: pointer;
`;
